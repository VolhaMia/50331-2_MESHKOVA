@model IEnumerable<_50331_2_MESHKOVA.Models.MenuItem>
<ul class="nav nav-pills">
    @foreach (var item in Model)
    {
        <li class="@item.Active">@Html.ActionLink(item.Name, item.Action, item.Controller)</li>
    }
</ul>
